# REST API Endpoints


Find pet by Id:

```bash
GET: /api/pet/{petId} 
```

Add a new pet to the store:

```bash
POST: /api/pet
```

Update an existing pet:

```bash
PUT: /api/pet/{petId}
```

Delete a Pet:

```bash
DELETE: /api/pet/{petId}
```

